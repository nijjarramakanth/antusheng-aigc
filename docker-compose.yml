services:
  backend:
    build: .
    environment:
      FLASK_ENV: development
      FLASK_DEBUG: 1
      DATABASE_URL: sqlite:///aigc_education.db
      JWT_SECRET_KEY: your-jwt-secret-key-here
      SECRET_KEY: your-secret-key-here
    ports:
      - "5000:5000"
    volumes:
      - .:/app
